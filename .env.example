# Export only the providers you use. The runtime does not load .env files itself.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
MISTRAL_API_KEY=
# Ollama defaults to this local URL when the variable is unset.
OLLAMA_BASE_URL=http://localhost:11434
